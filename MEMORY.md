---
title: 方案 B 五大核心指令 — L3 持久記憶層記錄
date: 2026-05-14
description: 複製本文内容至 .claude/projects/[vault]/memory/MEMORY.md 的「永久指示」章節
---

# 【L3 持久記憶層】方案 B 五大核心指令完整記錄

> **說明**：以下內容應複製至 MEMORY.md 檔案中的「🔴 永久指示（絕對不可遺漏）」章節下，作為跨會話的自動加載記憶。

---

## 📌 五大核心工作流指令（方案 B 完整系統）

### 1️⃣ `/練化[文件名]` — 精煉 + 晉升 + 自動歸檔

**觸發**：有需要精煉的文件時（隨時）

**完整七階段流程**（自動執行，無中斷）：
- Step_0：全路徑搜尋檔案 + 用戶確認位置
- **Step_0.5：代碼層預檢（Frontmatter、iframe、Mermaid、YouTube Video ID 補充）** ← 2026-05-21 新增
- Step_1-3：修正三項缺陷
- Step_4.5：代碼層驗證
- Step_4.9：建立雙向 Wiki 連結
- Step_5：強制歸檔（移動至 raw/archive/、更新統計、記錄日誌）
- Step_6：輸出精煉報告

**Step_0.5 YouTube Video ID 補充流程（2026-05-21 新增）**：
- ✅ **檢查項目**：Frontmatter 中的 `video_url` 和 `video_id` 是否含有 `[video_id]` 佔位符
- ✅ **若有佔位符**：
  1. 向用戶詢問並取得實際 YouTube 連結（例如：`https://youtu.be/eHIpMclCtbo`）
  2. 從連結中提取 Video ID（`youtu.be/` 或 `watch?v=` 後的部分）
  3. 自動更新四個位置：
     - Frontmatter line (video_url)：`https://www.youtube.com/watch?v=[VIDEO_ID]`
     - Frontmatter line (video_id)：`[VIDEO_ID]`
     - Callout 中的備用連結：`https://www.youtube.com/watch?v=[VIDEO_ID]`
     - iframe src：`https://www.youtube.com/embed/[VIDEO_ID]`
  4. 同步更新 `raw/archive/` 中的存檔版本
  5. 驗證所有連結格式正確，視頻播放正常
- ✅ **若無佔位符**：通過檢查，進入 Phase 1

**關鍵規則**：
- ✅ Step_0 等待用戶確認檔案位置
- ✅ Step_0.5-5.4 連續自動執行，無需等待（若需補充 Video ID，詢問用戶後立即補充）
- ✅ Step_4.9 + Step_5 強制執行，不可跳過
- ❌ 禁止等待用戶確認後再執行歸檔
- ❌ 禁止留下 [video_id] 佔位符進入 Phase 1（影片會無法播放）

---

### 2️⃣ `/轉存 [法令名稱]` — 法令轉化 + 自動存檔至 8 層資料夾

**觸發**：需要將完整法令存檔至知識庫時

**完整五階段流程**（自動執行）：
- Phase 1：Gemini 智能理解法令內容
- Phase 2：轉化為結構化 Markdown（條文、解釋、應用）
- Phase 3：智能判斷 8 層正確資料夾位置
- Phase 4：自動生成 Frontmatter（title、tags、moc、related_kb）
- Phase 5：存檔至知識庫 + 自動更新所有索引和日誌

**8 層資料夾對應**：
| 層級 | 資料夾名稱 | 適用法規 |
|------|----------|--------|
| 01 | 通用法規與基礎 | 警察法、行政程序法 |
| 02 | 民防防空與防災體系 | 民防法、防空、災害防救 |
| 03 | 槍械刀械與原住民獵槍 | 槍械管制、獵槍、刀械 |
| 04 | 維安反恐與關鍵基礎設施 | 反恐、全民防衛、關鍵設施保護 |
| 05 | 選舉治安與集會遊行 | 選舉法、集會遊行法 |
| 06 | 監視系統與個資 | 監視器法制、個資保護法 |
| 07 | 精神衛生與山區 | 精神衛生法、山區安全 |
| 08 | 跨域與其他法規 | 跨域性質特定法規 |

**關鍵規則**：
- ✅ 使用 Gemini 智能轉化（非手動複製）
- ✅ Phase 3 自動判斷正確層級（無須手動分類）
- ✅ Phase 5 強制執行（必須更新所有 index.md、log.md）
- ❌ 禁止存檔後忘記更新層級索引

---

### 3️⃣ `/wiki-ingest` — 知識攝取（週末用）

**觸發**：週末知識重整時

**完整四階段流程**（自動執行）：
- Phase 1：掃描 raw/ 中 `status: new` 的檔案
- Phase 2：使用晉升決策速查表自動分類至 4 大 MOC
- Phase 3：晉升至知識庫 + 建立雙向連結
- Phase 4：歸檔原始檔 + 更新所有統計和日誌

**4 大 MOC**：
1. AI工具應用
2. 保安業務
3. 國學與傳統智慧
4. 法規研析

---

### 4️⃣ `/wiki-query [主題]` — 知識查詢（決策支援）

**觸發**：需要快速查詢知識支援決策時（頻繁）

**完整四階段流程**（自動執行）：
- Phase 1：分析用戶查詢主題
- Phase 2：在 4 大 MOC 深度搜尋相關頁面
- Phase 3：綜合多個知識點 + 標註信息來源
- Phase 4：有價值的洞見自動存入 syntheses/ 資料夾

**常用查詢場景**：
- 準備長官會議 → `/wiki-query 主題相關的風險評估與策略`
- 設計培訓課程 → `/wiki-query 基層員警教導的最佳實踐`
- 評估新專案 → `/wiki-query 相關法規標準與成功案例`

---

### 5️⃣ `/wiki-lint` — 知識庫巡檢（月底用）

**觸發**：每月月底定期維護

**完整四階段流程**（自動執行）：
- Phase 1：死鏈檢測（掃描指向不存在的連結）
- Phase 2：孤島檢測（找出無任何連結指向的頁面）
- Phase 3：完整性檢查（缺失 frontmatter、index.md、log.md）
- Phase 4：報告生成 + 優先修復清單

---

## 📊 五大指令使用頻率與時機

| 指令 | 頻率 | 時機 | 優先級 |
|------|------|------|--------|
| `/練化` | 隨時 | 有新文件需精煉 | P1 |
| `/轉存` | 定期 | 收集到完整法令 | P1 |
| `/wiki-query` | 頻繁 | 決策、報告、會議 | P2 |
| `/wiki-ingest` | 每週末 | 知識重整 | P3 |
| `/wiki-lint` | 每月底 | 定期維護 | P4 |

---

## ✅ 跨設備使用流程（通用）

```
家裡桌機 → 完成某項工作 → /練化 或 /轉存
↓
(Vault 同步)
↓
辦公室桌機 → 進入 vault → MEMORY.md 自動加載
↓
執行相同指令，流程完全相同 ✅
```

**無須任何配置或記憶細節，完全自動化。**

---

## 🔴 永久提醒（不可遺漏）

### 🔴 【絕對最高優先級】執行任何指令前必讀 CLAUDE.md（2026-05-21 用戶強制指示）

**強制規則**：在執行任何 Claude 工作指令前（包括 `/練化`、`/轉存`、`/wiki-ingest`、`/wiki-query`、`/wiki-lint`、`/簡報大綱製作` 等），**必須優先讀取 CLAUDE.md 文件**，確保：
1. 了解該指令 the 最新流程與預檢查清單
2. 掌握是否有新增規則、修正或補充（如 2026-05-21 新增的 YouTube Video ID 補充流程）
3. 防止遺漏任何強制規範或禁止事項

**實施理由**（2026-05-21 發現）：
- Step_0.5 預檢查清單已於 2026-05-21 新增「YouTube Video ID 補充」流程
- 此前執行的 `/練化` 操作未包含此項檢查，導致視頻無法播放
- 為防止同類缺陷重複發生，所有指令執行前**必須先檢查 CLAUDE.md 的最新版本**

**執行方式**：
```
任何指令觸發
  ↓
✅ 讀取 CLAUDE.md（該指令對應的段落，特別注意日期標記）
  ↓
✅ 確認所有預檢查清單項目
  ↓
✅ 然後開始執行 Phase 1 → Phase N
```

**時機檢查點**：
- 每次開始新的 `/練化`、`/轉存` 等指令前
- 長時間對話中每隔約 20-30 分鐘重新掃描 CLAUDE.md 更新
- 跨會話時（新對話開始）立即讀取

---

### 🚀 【最高優先級】執行前必檢查清單（2026-05-20 用戶明確指示）

**新規則**：每次執行 Claude 工作前，**第一步必須執行**：
```
Please follow the rules in CLAUDE.md in this repository as you work on [task].
```

**執行時機**：
- ✅ 開始 any 新任務前（無例外）
- ✅ 跨會話時重新加載（MEMORY 自動加載後立即檢查）
- ✅ 涉及 vault 操作時必須執行
- ✅ 長時間對話中每隔一定時間（約 20 分鐘）重新檢查一次

**作用**：
- 確保 CLAUDE.md 中的所有規則得到完整理解和遵守
- 防止違反「強制規範」和「流程圖」中的任何指示
- 保護知識庫的結構完整性和操作安全性

---

### 📋 其他永久提醒

- ✅ **【2026-05-23 新增】建立雙向連結與防止孤島**：在 Obsidian 內建立任何檔案時，**除了完成自動歸檔，必須同時建立雙向連結 (Wikilinks)**。確保新檔案包含 `moc` 指向和至少 3 個 `related` 相關節點，並在對應的主 MOC 或相關父級頁面中同步加入該檔案連結，實現雙向互聯互通。
- ✅ `/練化` 和 `/轉存` 的 Phase 5 必須強制執行歸檔和索引更新
- ✅ `/wiki-ingest` 每週末至少執行一次，防止 raw/ 堆積
- ✅ `/wiki-lint` 每月底執行一次，保持知識庫健康
- ✅ `/wiki-query` 用於決策支援，積極使用，最大化知識資產價值
- ❌ 禁止任何指令在中途等待用戶確認（除了 Step_0）

---

### 🎥 【2026-05-22 新增】影片筆記 iframe 嵌入檢查自動化（永久優先級）

**背景缺陷**（2026-05-22 用戶發現）：
- 檔案「Obsidian 與 AI 整合的五大高效模式終極指南」標記為影片筆記，但缺少 YouTube iframe 嵌入
- 系統之前未能自動檢查並修正此缺陷，導致影片無法播放
- 用戶明確指示：「這個缺失能否都記住，並填入/練化指令中永遠能自我檢視修正，不要我每次提醒有沒有崁入影片」

**永久化規則**（已在 CLAUDE.md Step_0.5 預檢查清單中實施）：
- ✅ **新增檢查項 1.5**：「影片筆記強制檢查（2026-05-22 新增 — 用戶實踐發現）」
- ✅ **檢查項內容**：IF `type: 影片筆記` THEN 強制檢查以下三項：
  1. ✅ `video_url` 欄位：是否含有有效 YouTube 連結
  2. ✅ `video_id` 欄位：是否含有有效 Video ID（非佔位符）
  3. ✅ iframe 嵌入：是否正確嵌入 YouTube 播放器（HTML5 完整標籤）
- ✅ **強制拘束**：禁止聲稱「已驗證」但實際檔案無 iframe 嵌入的情況

**執行流程**（自動化）：
```
/練化 [影片筆記檔案]
  ↓
Step_0.5 預檢查 → 偵測 type=影片筆記
  ↓
自動驗證三項：Frontmatter + Video ID + iframe
  ↓
若缺漏：自動修正 + 添加完整 HTML5 iframe
  ↓
同步更新知識庫版本 + archive 版本
  ↓
✅ COMPLETE（無需用戶每次提醒）
```

**詳細規則位置**：CLAUDE.md → Step_0.5 預檢查審計檢查清單 → 檢查項 1.5（新增）

---

## 📝 複製位置說明

**將上述內容複製至**：
```
.claude/projects/[vault-path]/memory/MEMORY.md

在以下章節下新增：

## 🔴 永久指示（絕對不可遺漏）

### 5️⃣ 【五大核心工作流指令】方案 B 完整系統

[上述內容複製到此]
```

---

**完成日期**：2026-05-14  
**狀態**：✅ 三層級記錄系統完整建立

## 🔗 逆向連結 (Backlinks)
- [[Claudian快捷面板]]
- [[2026-05-14]]
- [[2026-05-04]]
- [[2026-05-10]]
- [[2026-05-03]]
